{
  "name": "my-vrchat-app",
  "version": "1.0.0",
  "description": "Application VRChat avec intégration Netlify",
  "scripts": {
    "build": "your-build-command-here",
    "start": "node server.js"  // Exemple de script de démarrage pour une application Node.js (ajustez selon votre cas)
  },
  "dependencies": {
    "express": "^4.17.1",     // Exemple de dépendance pour une application Node.js
    "axios": "^0.21.1"        // Exemple de dépendance pour effectuer des requêtes HTTP
  },
  "devDependencies": {
    "typescript": "^4.3.5"    // Exemple de dépendance de développement, si nécessaire
  }
}
